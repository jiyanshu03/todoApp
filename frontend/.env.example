# API base URL (only needed when not using default localhost)
# Development: leave unset to use http://localhost:3000/api
# Production (e.g. Vercel): set to your deployed backend URL
# VITE_API_URL=https://your-backend.vercel.app/api
