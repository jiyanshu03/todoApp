# Copy this file to .env and fill in for local or production.
# Frontend env – Vite only exposes variables prefixed with VITE_

# API base URL
# - Local dev: leave unset or empty → uses http://localhost:3000/api
# - Production (Vercel): set to your deployed backend URL + /api (no trailing slash)
# VITE_API_URL=https://your-backend.vercel.app/api
